# Backend API base URL
# VITE_API_BASE_URL=https://your-backend.domain

# Endpoint overrides are optional. By default, the app uses:
# /auth/email/send
# /auth/email/verify
# /auth/signup
# /auth/login
# /auth/reissue
# /auth/logout

# 개발용 임시 모드(백엔드 없을 때 true)
# true면 실제 auth API 호출 없이 mock 응답으로 동작
VITE_MOCK_AUTH_API=false
VITE_MOCK_AUTH_DELAY_MS=500
VITE_MOCK_VERIFICATION_CODE=123456
VITE_MOCK_VERIFICATION_DELAY_MS=500

# 개발용 User API mock 모드(백엔드 연결이 어려울 때 true)
# true면 /user/me, /user/me/achievement 등이 mock 응답으로 동작
VITE_MOCK_USER_API=false
VITE_MOCK_USER_DELAY_MS=300

# 개발용 Subscription API mock 모드(백엔드 연결이 어려울 때 true)
# true면 /subscription/plan이 mock 응답으로 동작
VITE_MOCK_SUBSCRIPTION_API=false
VITE_MOCK_SUBSCRIPTION_DELAY_MS=300
