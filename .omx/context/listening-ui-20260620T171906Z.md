# Context Snapshot: Listening UI Alignment

Task statement: Listening 화면 UI를 첨부된 Figma 이미지에 맞게 수정하기 위한 deep-interview.
Desired outcome: ListeningLesson_01/02/03 상태가 첨부 이미지처럼 보이도록 색상, 배치, 상태 표현 범위를 명확히 한다.
Stated solution: Listening 화면 UI 수정.
Probable intent hypothesis: Reading 화면과 같은 수준으로 Listening 화면도 Figma 시안의 기본/텍스트 보기 전/텍스트 보기 후/완료 상태를 맞추려는 목적.
Known facts/evidence:
- Listening UI is implemented in `src/pages/GrammarPracticePage.tsx` under `practiceStep === 'listening'`.
- Listening styles are in `src/pages/GrammarPracticePage.css` using `grammar-practice-listening-*` selectors.
- Listening currently reuses some `grammar-practice-reading-*` classes for toggles, question cards, answer buttons, dots, and Next button.
- The attached image shows three main states: base audio/question state, Show text available state, and transcript-visible/Next-active state.
- Project rule in `AGENTS.md` and `docs/design-system.md`: web UI colors should use `--dojeon-color-*` variables from `src/index.css`, not hard-coded colors.
Constraints:
- Need avoid breaking Reading changes just committed.
- Need use existing CSS token system.
- No new dependency is implied.
Unknowns/open questions:
- Whether the pass should cover only color/layout or also interaction behavior such as Show text readiness and Next activation.
- Whether Listening should be fully style-separated from Reading shared classes.
- Whether answer correctness feedback states are in scope for Listening, since the image only shows selected/active Next states.
Decision-boundary unknowns:
- Whether implementation may add Listening-only wrapper/classes to prevent Reading regressions.
- Whether exact pixel matching can be approximated with current CSS variables and structure.
Likely codebase touchpoints:
- `src/pages/GrammarPracticePage.tsx`
- `src/pages/GrammarPracticePage.css`
- Possibly `src/index.css` only if an existing token is missing.
Relevant repo docs/rules/context inspected:
- `AGENTS.md`
- `README.md`
- `docs/design-system.md`
- `.omx/specs/deep-interview-reading-color-ui.md`
Terminology/doc conflicts found: none.
Prompt-safe initial-context summary status: not_needed.
